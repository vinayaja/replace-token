name: "Push workflow"
description: "Push workflow in remote or local repository"
author: "Vinayak Jadhav"

inputs:
  name:
    description: "Name"
    required: true
    default: "Hello"

runs:
  using: "docker"
  main: "src/script.ps1"